{
  "name": "@vpa/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Tipos, enums y schemas Zod compartidos para VPA",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "require": "./src/index.ts"
    }
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "test:unit": "vitest run --reporter=verbose",
    "test:property": "vitest run --reporter=verbose"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "fast-check": "^3.20.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
